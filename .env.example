# Batitrack secrets

# Public/anon key — safe to commit and embed in client code.
SUPABASE_URL=https://yaarnpduwcsmrxmrhrvw.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_ShDSm2hH-O6Hq1_Qzf9E3A_yn0GEaFh

# NEVER commit the service_role key. It bypasses RLS entirely.
# Keep it only on a server / CI runner / your local .env (not in this repo).
# SUPABASE_SERVICE_ROLE_KEY=<KEEP THIS OFF GITHUB>
