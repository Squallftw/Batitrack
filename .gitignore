# macOS
.DS_Store

# Local env (never commit secrets)
.env
.env.local

# Editor
.vscode/
.idea/

# Build artefacts (none yet, but reserve)
dist/
build/

# Screenshots used during development
screenshots/
*.png.tmp

# Versioned local copies
Batitrack-source.html
"Batitrack (standalone).html"

# Node modules — if anyone adds tooling later
node_modules/
